version: '3.2'
services:
  rabbitmq:
    image: rabbitmq:3
    container_name: rabbitmq
    hostname: my-rabbit
    environment:
      RABBITMQ_SERVER_ADDITIONAL_ERL_ARGS: "-rabbit heartbeat 0"
    ports:
      - 5672:5672
      - 15672:15672